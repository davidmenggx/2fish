#pragma once

#include "imgui.h"

namespace theme {
static constexpr ImU32 RED_FOCUSED = IM_COL32(255, 91, 122, 255);
static constexpr ImU32 RED_UNFOCUSED = IM_COL32(255, 91, 122, 45);

static constexpr ImU32 GREEN_FOCUSED = IM_COL32(0, 200, 129, 255);
static constexpr ImU32 GREEN_UNFOCUSED = IM_COL32(0, 200, 129, 45);
} // namespace theme
